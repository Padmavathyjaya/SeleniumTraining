package org.stepdefinition;

import org.base.BaseClass;
import org.base.FbLoginPojo;

import cucumber.api.java.en.Given;
import cucumber.api.java.en.Then;
import cucumber.api.java.en.When;

public class FaceBooklogin extends BaseClass {

	FbLoginPojo f;

	/*
	 * @Given("TO launch the  chrome browser and maximise the window") public void
	 * to_launch_the_chrome_browser_and_maximise_the_window() { launchBrowser();
	 * windowMaximize(); }
	 * 
	 * @When("launch the url") public void launch_the_url() {
	 * launchurl("https://www.facebook.com/"); }
	 * 
	 * @When("pass the data in {string} in email field") public void
	 * pass_the_data_in_in_email_field(String email) { f = new FbLoginPojo();
	 * passText(email, f.getEmail()); }
	 * 
	 * @When("pass the data in {string} password field") public void
	 * pass_the_data_in_password_field(String pass) { f = new FbLoginPojo();
	 * passText(pass, f.getPassword()); }
	 * 
	 * @When("click the login button") public void click_the_login_button() { f =
	 * new FbLoginPojo(); clickBtn(f.getLogin());
	 * 
	 * }
	 * 
	 * @When("check whether navigate to the home page or not") public void
	 * check_whether_navigate_to_the_home_page_or_not() { System.out.
	 * println("validate the positive and negative combination of  login  functionality"
	 * ); }
	 * 
	 * @Then("close the browser") public void close_the_browser() {
	 * closeEntireBrower();
	 * 
	 * }
	 */
}
